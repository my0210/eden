# Supabase Configuration
# Get these values from your Supabase project settings: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Cron Job Configuration (optional but recommended for security)
# Set a secret token to protect your cron endpoints from unauthorized access
# Generate a secure random string: openssl rand -hex 32
CRON_SECRET=your_cron_secret_here

# Worker Configuration
# Secret token for Railway worker to call internal Vercel endpoints
# Generate a secure random string: openssl rand -hex 32
WORKER_SECRET=your_worker_secret_here

# Supabase Service Role Key (optional, for admin operations)
# Only needed if you want to bypass RLS for internal operations
# Get from: https://app.supabase.com/project/_/settings/api
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

